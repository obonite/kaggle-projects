# ---------------------------------------------------
# Python Compiled Files and Cache
# ---------------------------------------------------
*.pyc # Python compiled files
__pycache__/ # Python bytecode cache directory

# ---------------------------------------------------
# Jupyter Notebook Specific Ignores
# ---------------------------------------------------
.ipynb_checkpoints/ # Jupyter Notebook checkpoints directory

# ---------------------------------------------------
# Python Virtual Environments (CRITICAL TO KEEP FOR REPRODUCIBILITY)
# These folders contain the installed packages for a specific project.
# They should NOT be committed to Git.
# ---------------------------------------------------
.venv/ # Common name for virtualenv environments
venv/ # Another common name for virtualenv environments
env/ # Yet another common name
ENV/
env.bak/ # If you ever backup an env directory
build/ # Used by setuptools
dist/ # Used by setuptools
*.egg-info/ # Generated by setuptools for packages
.Python # macOS specific Python related files (sometimes generated)
.python-version # pyenv specific version file

# ---------------------------------------------------
# Data and Models (General Patterns - Adjust as needed)
# IMPORTANT: Use Git LFS for large datasets/models that need versioning.
# These ignores are for data you *don't* want in Git at all,
# or for processed/intermediate data that can be regenerated.
# ---------------------------------------------------
data/processed/ # Processed/intermediate data (can usually be regenerated)
data/temp/
output/
results/
predictions/
submission.csv # Often a final output that changes frequently

models/ # Trained models (can be large, and often regenerated. Use LFS if versioning needed.)
*.model
*.weights
*.pt # PyTorch model files
*.onnx
*.pb # TensorFlow Protobuf files

# ---------------------------------------------------
# Logs and Runtime Files
# ---------------------------------------------------
*.log
logs/
nohup.out

# ---------------------------------------------------
# Configuration files (if sensitive or machine-specific)
# ---------------------------------------------------
.env # Common for environment variables (e.g., API keys) - DO NOT COMMIT SENSITIVE INFO!
config.ini.local # Local overrides not for repo
secrets.py # Python file for secrets (use .env or proper secret management)

# ---------------------------------------------------
# Jupyter Lab / IPython history
# ---------------------------------------------------
.ipython/
.jupyter/

# ---------------------------------------------------
# Specific to some ML frameworks/tools
# ---------------------------------------------------
# MLflow
mlruns/

# DVC (Data Version Control - if you use it for large data management)
.dvc/
*.dvc
